# دليل الإعداد - AMA Order System

## 📋 نظرة عامة

هذا الملف يشرح كيفية تشغيل النظام في بيئتين مختلفتين:
1. **بيئة الاختبار المحلية** (للتطوير والاختبار داخل المكتب)
2. **بيئة الإنتاج** (للوصول من الإنترنت للعمال)

> **ℹ️ ملاحظة:**  
> النظام يستخدم HTTPS مع شهادات SSL للتشفير. قد تحتاج لقبول تحذير الأمان في المتصفح عند الوصول لأول مرة.

---

## 🌐 معلومات الشبكة

- **Local IP**: 10.10.10.110 (عنوان الجهاز داخل الشبكة المحلية)
- **Public IP**: 213.6.226.163 (عنوان الشبكة من الإنترنت)
- **Domain**: `amaorders.work` (النطاق الجديد)

### 📊 جدول البيئات والبورتات

| البيئة | Frontend | Backend | الوصول المحلي | الوصول من الإنترنت | الاستخدام |
|--------|----------|---------|---------------|-------------------|-----------|
| **🧪 الاختبار** | 5174 | 3004 | https://10.10.10.110:5174 | https://amaorders.work:5174 | للتطوير والاختبار |
| **🌍 الإنتاج** | 5173 | 5001 | https://10.10.10.110:5173 | https://amaorders.work:5173 | للعمال (الاستخدام الفعلي) |

**Port Forwarding المطلوب على الراوتر:**
```
# للاختبار
External 5174 → Internal 10.10.10.110:5174 (TCP/UDP)
External 3004 → Internal 10.10.10.110:3004 (TCP/UDP)

# للإنتاج
External 5173 → Internal 10.10.10.110:5173 (TCP/UDP)
External 5001 → Internal 10.10.10.110:5001 (TCP/UDP)
```

## 🧪 بيئة الاختبار المحلية (للمطورين)

> **متى تستخدم هذه البيئة؟**  
> عندما تريد الاختبار داخل المكتب دون التأثير على العمال. البورتات 5174 و 3004 مخصصة للاختبار فقط.

### البورتات المستخدمة
- **Frontend**: 5174
- **Backend**: 3004

### الخطوات

#### 1️⃣ تشغيل الـ Backend (Server)

```bash
cd server
npm run test
```

**ماذا يحدث؟**
- الأمر `npm run test` يشغل السيرفر على البورت 3004 (محدد في `package.json`)
- السيرفر يقرأ الإعدادات من ملف `server/.env` (PORT=3004)

**تأكد من الرسالة:**
```
HTTPS Server running on https://localhost:3004
Also accessible at https://10.10.10.110:3004
```

#### 2️⃣ تشغيل الـ Frontend

```bash
cd ama-order-system-front

# تشغيل على البورت 5174 (وضع الاختبار)
npm run test
```

**ماذا يحدث؟**
- `npm run test` يشغل Vite على البورت 5174 بوضع الاختبار
- يقرأ الإعدادات من `.env.test`
- `.env.test` فيه `VITE_API_URL=https://10.10.10.110:3004`
- الفرونت إند يتصل بالسيرفر على البورت 3004

#### 3️⃣ الوصول للتطبيق

**من داخل الشبكة المحلية:**
```
https://10.10.10.110:5174
```

**من الإنترنت (بعد فتح البورتات):**
```
https://213.6.226.163:5174
```

---

## 🌐 بيئة الاختبار من الإنترنت (للمطورين خارج المكتب)

> **متى تستخدم هذه البيئة؟**  
> عندما تريد اختبار النظام من خارج المكتب (من البيت أو أي مكان) دون التأثير على بيئة الإنتاج.

### المتطلبات

#### Port Forwarding على الراوتر
يجب فتح البورتات التالية على الراوتر:
```
External Port 5174 → Internal IP 10.10.10.110:5174 (TCP/UDP)
External Port 3004 → Internal IP 10.10.10.110:3004 (TCP/UDP)
```

**كيف تطلب من شركة الإنترنت:**
> "نحتاج Port Forwarding للبورتات التالية:
> - البورت الخارجي **5174** يوجّه للجهاز الداخلي **10.10.10.110** على البورت **5174**
> - البورت الخارجي **3004** يوجّه للجهاز الداخلي **10.10.10.110** على البورت **3004**
> - النوع: **TCP و UDP** (الاثنين)"

### الخطوات

#### 1️⃣ تشغيل الـ Backend

السيرفر يشتغل بنفس الطريقة (البورت 3004):
```bash
cd server
npm run test
```

#### 2️⃣ تشغيل الـ Frontend

نفس الخطوات السابقة:
```bash
cd ama-order-system-front
npm run test
```

#### 3️⃣ الوصول من الإنترنت

افتح من أي مكان (حتى من موبايلك بالبيانات):
```
https://213.6.226.163:5174
```

#### 4️⃣ التحقق من البورتات

اختبر إذا البورتات مفتوحة:
```bash
# اختبار البورت 3004 (Backend)
curl -k https://213.6.226.163:3004/api/auth/verify

# اختبار البورت 5174 (Frontend)
curl -k https://213.6.226.163:5174
```

### ⚠️ ملاحظات مهمة

- ✅ البورتات 5174 و 3004 للاختبار فقط
- ✅ لا تؤثر على بيئة الإنتاج (5173/5001)
- ✅ يمكن تشغيل البيئتين معاً في نفس الوقت
- ⚠️ تذكر قبول تحذير SSL عند الوصول لأول مرة

---

## 🌍 بيئة الإنتاج (للعمال - الوصول من الإنترنت)

> **متى تستخدم هذه البيئة؟**  
> عندما تريد أن يصل العمال للنظام من هواتفهم أو أجهزتهم خارج المكتب.

### البورتات المستخدمة
- **Frontend**: 5173
- **Backend**: 5001

### الخطوات

#### 1️⃣ تحديث إعدادات الـ Backend

عدّل ملف `server/.env`:
```env
PORT=5001
```

#### 2️⃣ تشغيل الـ Backend

```bash
cd server
npm run test
```

**تأكد من الرسالة:**
```
HTTPS Server running on https://localhost:5001
Also accessible at https://10.10.10.110:5001
```

#### 3️⃣ بناء وتشغيل الـ Frontend

```bash
cd ama-order-system-front

# بناء المشروع بوضع الإنتاج
npm run build

# تشغيل على البورت 5173
npm run prod
```

**ماذا يحدث؟**
- `npm run build` يبني المشروع بوضع الإنتاج
- يقرأ من `.env.production` الذي فيه `VITE_API_URL=https://213.6.226.163:5001`
- `npm run prod` يشغل Vite preview على البورت 5173
- الفرونت إند يتصل بالسيرفر عبر العنوان العام

#### 4️⃣ إعداد DNS (GoDaddy)
1. اذهب إلى إعدادات DNS للنطاق `amaorders.work`
2. أضف سجل (Record) جديد:
   - **Type**: A
   - **Name**: @
   - **Value**: 213.6.226.163
   - **TTL**: 600 seconds (أو أقل قيمة ممكنة)

#### 5️⃣ إعداد Port Forwarding على الراوتر

تأكد من الإعدادات التالية على الراوتر:
```
External Port 5173 → Internal IP 10.10.10.110:5173 (TCP/UDP)
External Port 5001 → Internal IP 10.10.10.110:5001 (TCP/UDP)
```

#### 6️⃣ الوصول للتطبيق

**من داخل الشبكة المحلية:**
```
https://10.10.10.110:5173
```

**من الإنترنت (للعمال):**
```
https://amaorders.work:5173
```

---

## 📱 تثبيت التطبيق (PWA)

### على الهواتف:
1. افتح الرابط في المتصفح
2. اقبل تحذير الأمان (SSL warning) إذا ظهر
3. اضغط على "إضافة إلى الشاشة الرئيسية"
4. التطبيق سيظهر كأيقونة على الشاشة

### على الكمبيوتر:
1. افتح الرابط في Chrome
2. اقبل تحذير الأمان (SSL warning) إذا ظهر
3. اضغط على أيقونة التثبيت في شريط العنوان
4. أو: القائمة → More Tools → Create Shortcut → ✓ Open as window

---

## 📂 شرح ملفات الإعدادات

### ملفات الـ Backend

**`server/.env`**
```env
PORT=3004  # أو 5001 حسب البيئة
JWT_SECRET=your_jwt_secret_key
DB_NAME=ama_order_system
DB_USER=postgres
DB_PASSWORD=9630
DB_HOST=localhost
DB_PORT=5432
EMAIL_USER=amacompany92@gmail.com
EMAIL_PASSWORD=zbwkhpkpmngdqylw
```

**`server/package.json`**
```json
"scripts": {
  "test": "PORT=3004 nodemon index.js"  // يشغل على البورت 3004
}
```

### ملفات الـ Frontend

**`.env`** (للتطوير العادي)
```env
VITE_API_URL=https://10.10.10.110:3004
```

**`.env.test`** (للاختبار المحلي)
```env
VITE_API_URL=https://10.10.10.110:3004
```

**`.env.production`** (للإنتاج - الإنترنت)
```env
VITE_API_URL=https://213.6.226.163:5001
```

**`vite.config.js`**
```javascript
server: {
  port: 5174,  // البورت الافتراضي للتطوير
  https: { /* شهادات SSL */ }
}
```

**`package.json`**
```json
"scripts": {
  "dev": "vite",  // تطوير عادي على البورت الافتراضي
  "build": "vite build",  // بناء للإنتاج
  "test": "vite --port 5174 --mode test",  // اختبار على البورت 5174
  "prod": "vite preview --port 5173 --host"  // إنتاج على البورت 5173
}
```

---

## 🔧 استكشاف الأخطاء

### مشكلة: "Failed to fetch" أو "ERR_CERT_AUTHORITY_INVALID"

**السبب:** التطبيق المبني يستخدم عنوان API خاطئ

**الحل:**
1. تأكد من استخدام الأمر الصحيح:
   - للاختبار: `npm run test`
   - للإنتاج: `npm run build` ثم `npm run prod`
2. أعد تشغيل السيرفر

### مشكلة: البورت مشغول

**الحل:**
```bash
# أوقف العملية على البورت
lsof -ti:5174 | xargs kill -9
lsof -ti:3004 | xargs kill -9
```

### مشكلة: لا يمكن الوصول من الإنترنت

**تحقق من:**
1. Port Forwarding على الراوتر
2. Firewall على الجهاز المضيف
3. السيرفر يعمل على البورت الصحيح

3. السيرفر يعمل على البورت الصحيح

### ⚠️ مشكلة عدم فتح الرابط الخارجي وأنت داخل الشبكة (NAT Loopback)

إذا كان الرابط الخارجي (`https://213.6.226.163...`) يعمل من الـ 4G ولكن **لا يعمل** وأنت متصل بشبكة الواي فاي الداخلية، فهذا طبيعي ويسمى **NAT Loopback Issue**.

**السبب:**
معظم الراوترات المنزلية تمنع الأجهزة الداخلية من الوصول لنفسها عبر العنوان الخارجي (Public IP).

**الحلول:**
1. **الحل الأسهل:** استخدم الرابط الداخلي (`10.10.10.110`) وأنت في المكتب، والرابط الخارجي (`213.6.226.163`) وأنت في الخارج.
2. **إعدادات الراوتر:** ابحث في إعدادات الراوتر عن خيار اسمه `NAT Loopback` أو `Hairpinning` وقم بتفعيله.
3. **استخدام Domain Name (الحل الاحترافي):**
   - احجز اسم نطاق مجاني (مثل `my-ama-system.ddns.net`) واربطه بالـ IP الخارجي.
   - في أجهزة الكمبيوتر داخل المكتب، عدّل ملف `hosts` ليربط هذا النطاق بالـ IP الداخلي `10.10.10.110`.
   - بهذه الطريقة تستخدم نفس الرابط في كل مكان.

---

## 🚀 الأوامر السريعة

### 1️⃣ للاختبار المحلي (داخل المكتب)

**البورتات:** 3004/5174

```bash
# Terminal 1 - Backend
cd server
npm run test

# Terminal 2 - Frontend
cd ama-order-system-front
npm run test
```

**الرابط:** `https://10.10.10.110:5174`

---

### 2️⃣ للاختبار من الإنترنت (من البيت أو خارج المكتب)

**البورتات:** 3004/5174  
**⚠️ يتطلب:** Port Forwarding للبورتات 5174 و 3004

```bash
# نفس الأوامر السابقة
# Terminal 1 - Backend
cd server
npm run test

# Terminal 2 - Frontend
cd ama-order-system-front
npm run test
```

**الرابط من الإنترنت:** `https://213.6.226.163:5174`

---

### 3️⃣ للإنتاج (للعمال)

**البورتات:** 5001/5173  
**⚠️ يتطلب:** Port Forwarding للبورتات 5173 و 5001

```bash
# Terminal 1 - Backend
cd server
# عدّل .env ليصير PORT=5001
npm run test

# Terminal 2 - Frontend
cd ama-order-system-front
npm run build
npm run prod
```

**الرابط للعمال:** `https://213.6.226.163:5173`

---

## ⚠️ ملاحظات مهمة

### 🔐 شهادات SSL
- النظام يستخدم HTTPS مع شهادات SSL للتشفير
- الشهادة المستخدمة هي self-signed (محلية)
- المستخدمون سيرون تحذير أمان في أول مرة
- اضغط "Advanced" ثم "Proceed" للمتابعة
- هذا طبيعي ولا يؤثر على الأمان داخل الشبكة المحلية

### 🌐 الشهادات متعددة النطاقات (Multi-Domain Certificate)
- الشهادة تدعم جميع العناوين:
  - `amaorders.work` (النطاق الجديد)
  - `10.10.10.110` (الشبكة المحلية)
  - `213.6.226.163` (الإنترنت)
  - `localhost` (التطوير المحلي)
- الشهادات صالحة لمدة **10 سنوات**

### 📂 ملفات الشهادات
- `server/certs/server-cert.pem` - شهادة السيرفر (متعددة النطاقات)
- `server/certs/server-key.pem` - مفتاح السيرفر الخاص

### الأمان
- ✅ تأكد من قوة كلمات المرور
- ✅ راقب الـ logs بانتظام
- ✅ البورتات 5173 و 5001 مفتوحة للإنترنت فقط عند الحاجة
- ✅ **لا تشارك المفتاح الخاص** (`server-key.pem`)

### التبديل بين البيئات
- **لا تنسَ** تغيير `PORT` في `server/.env` عند التبديل
- **لا تنسَ** استخدام الأمر الصحيح:
  - للاختبار: `npm run test`
  - للإنتاج: `npm run build` ثم `npm run prod`
- **لا تنسَ** استخدام ملفات الشهادات الجديدة (`server-cert.pem`, `server-key.pem`)
- **ملاحظة:** لا تستخدم `serve -s` لأنه لا يعمل بشكل صحيح مع ملفات HTML الثابتة
