import 'package:flutter/material.dart';

class AppColors {
  static const darkBackground = Color(0xFF0F172A);
  static const darkSurface = Color(0xFF1E293B);
  static const darkPrimary = Color(0xFF6366F1);
  static const darkAccent = Color(0xFFEC4899);
  static const darkTextMain = Color(0xFFF8FAFC);
  static const darkTextMuted = Color(0xFF94A3B8);
  static const darkGlassBorder = Color(0x1AFFFFFF);
  static const darkGlassBg = Color.fromRGBO(30, 41, 59, 0.7);
  static const darkInputFill = Color.fromRGBO(15, 23, 42, 0.6);

  static const statusCompleted = Color(0xFF34D399);
  static const statusInProgress = Color(0xFFFBBF24);
  static const statusEnteredErp = Color(0xFF8B5CF6);
  static const statusPending = Color(0xFF94A3B8);
  static const statusError = Color(0xFFEF4444);

  const AppColors._();
}
